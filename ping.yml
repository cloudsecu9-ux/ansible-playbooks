---
- name: Semaphore 및 Grit 연동 테스트용 Ping 플레이북
  hosts: all
  gather_facts: no

  tasks:
    - name: 1. Target 서버 연결 통신 테스트 (Ping)
      ansible.builtin.ping:

    - name: 2. 실행 완료 확인 로그 출력
      ansible.builtin.debug:
        msg: "🎉 [성공] Grit <-> Semaphore <-> Target 서버 통신이 정상적으로 연결되었습니다!"
